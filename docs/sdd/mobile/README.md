# SDD — mobile (Flutter)

Specs de telas e fluxos do app (`mobile/`).

Alinhar contratos com a API existente; não inventar backend paralelo no Dart.

Incluir: rotas (go_router), estados BLoC, auth (tokens no secure storage), o que é nativo (câmera, galeria) vs. o que só espelha o web.
